# Environment variables for portableralph Docker container
# Copy this file to .env.docker and fill in your actual API keys
# NEVER commit .env.docker with real keys to version control!

# =============================================================================
# OPENROUTER WHITELIST PROXY (RECOMMENDED)
# =============================================================================
# If you have an OpenRouter Whitelist proxy running on your host machine,
# configure it here. The proxy runs on port 4001 by default.

# OpenRouter Whitelist Proxy API Key (from your proxy's .env)
OPENROUTER_PROXY_KEY=your-proxy-api-key-here

# Proxy base URL (default: http://host.docker.internal:4001/v1)
# OPENROUTER_API_BASE=http://host.docker.internal:4001/v1

# =============================================================================
# MODEL CONFIGURATION
# =============================================================================

# Model alias to use (via OpenRouter Whitelist proxy)
# Available aliases: fast, smart, smartest, gemini, gemini-pro, gpt4, gpt4o, llama, cheap
# Default: 'fast' (DeepSeek v3 with fp8 quantization - cost-effective and fast)
CLAUDE_MODEL=fast
RALPH_DEFAULT_MODEL=fast

# For Claude Code to use the proxy:
# ANTHROPIC_BASE_URL=http://host.docker.internal:4001/v1
# ANTHROPIC_API_KEY=${OPENROUTER_PROXY_KEY}

# =============================================================================
# DIRECT API KEYS (ALTERNATIVE - without proxy)
# =============================================================================

# OpenRouter API Key (for direct OpenRouter access without whitelist proxy)
# Get your key at: https://openrouter.ai/keys
# OPENROUTER_API_KEY=your-openrouter-api-key-here

# Anthropic API Key (for direct Claude access)
# Get your key at: https://console.anthropic.com/
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# OpenAI API Key (if using OpenAI models directly)
# OPENAI_API_KEY=your-openai-api-key-here

# Google Gemini API Key (if using Gemini models)
# GEMINI_API_KEY=your-gemini-api-key-here

# =============================================================================
# RUNTIME CONFIGURATION
# =============================================================================

# Log level: debug, info, warn, error
RALPH_LOG_LEVEL=info

# Custom config directory (inside container)
RALPH_CONFIG_DIR=/home/ralphuser/.ralph
